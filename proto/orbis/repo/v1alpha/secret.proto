syntax = "proto3";
package orbis.repo.v1alpha;

// Stored secret
message Secret {
    string id = 1;
    bytes u = 2; // encryption commitment
    bytes nizk = 3; // proof of encryption
    string repoId = 4; // repo
}

// Share of a reencrypted secret
message SecretShare {
    
}

