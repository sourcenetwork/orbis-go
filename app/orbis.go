package app

import "context"

type OrbisNode struct{}

// NewNode returns a newly instanciated obris node
// configured with various services
func NewNode(ctx context.Context, opts ...Option) (*OrbisNode, error) {
	// todo
	return nil, nil
}
